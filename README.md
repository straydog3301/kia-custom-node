# Kia-Custom-Node

在此資料夾安裝 requirements.txt 的方式（假設 ComfyUI 根目錄中有 python.exe）：

- Windows CMD / PowerShell:
  ..\..\python.exe -m pip install -r requirements.txt

- 若要在 PowerShell 中確保以可執行路徑呼叫：
  & ..\..\python.exe -m pip install -r .\requirements.txt
